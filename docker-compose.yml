version: "3.9"
services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - 6379:6379
    volumes:
      - ./redis_data:/data
  redis_ui:
    image: redislabs/redisinsight
    depends_on:
      - redis
    container_name: redis_ui
    ports:
      - 8001:8001
