import styled from "styled-components";
import { Modal } from "react-bootstrap";

export const StyledModal = styled(Modal)`
  .modal-content {
    align-items: center;
    background-color: transparent;
    border: none;
  }
`;
export const StyledTransparentModelContent = styled(Modal.Body)``;
